# Utils

Utility helpers for the project.

- `config.py` — Load configuration and API key from `Data/API.env` or environment variables.
- `logger.py` — Returns a configured `logging.Logger` instance used across modules.

Place lightweight helpers here. Avoid heavy dependencies.
